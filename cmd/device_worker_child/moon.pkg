import {
  "vectie/lunaflux/engine/device_worker_child" @child,
  "vectie/lunaflux/internal/process",
}

supported_targets = "native"

pkgtype(kind: "executable")

options(
  link: { "native": { "cc-link-flags": "-pthread" } },
)
